#使用命令行初始化react-app
1. npm init react-app test-hooks && cd $_
  或者
   npx create-react-app test-hooks
2. npm info react
3. npm install --save --save-exact react{,-dom}@experimental
4. npm ls react










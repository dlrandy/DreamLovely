threejs 与webgl的关系？
threejs是用webgl来绘制3D；webgl是非常底层的系统，只能绘制点，线，角。

>  **直接使用webgl，则需要很多的code来处理scenes，lights,shadows,masterials,textures,3dmath等**


WebGLRenderer的作用？
负责接收你提供的数据并将数据渲染到canvas上

PerspectiveCamera
>threejs里的大多数角度都是弧度，但是perspective是度

BasicMaterial 不会对light产生反应
























